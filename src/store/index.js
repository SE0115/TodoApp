import { createStore } from 'vuex'
import axios from 'axios'

export default createStore({
    // data
    state: async () => ({
        todoEls: 
    }),
    // computed
    getters: {
       
    },
    // 변이 메소드 (데이터 수정 권한 o)
    mutations: {
        
    },
    // 액션 메소드 (데이터 수정 권한 x, 비동기)
    actions: {
        
    }
})